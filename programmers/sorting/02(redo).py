

# def solution(numbers):
#     numbers = list(map(str, numbers))  # numbers 배열을 string으로 변환
#     numbers.sort(key=lambda x: x*2, reverse=True)  # 문자열 2번 나열한걸 거꾸로 정렬
#     return str(int(''.join(numbers)))
